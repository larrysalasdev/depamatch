import AppShell from '@/components/app/AppShell';

export const metadata = {
  title: 'DepaMatch — El match perfecto para tu departamento en Lima',
  description: 'Analiza proyectos de departamentos en estreno en Lima con datos de BCRP, DataCrim, INDECOPI y SBS. Onboarding inteligente, comparador y simulador hipotecario.',
};

export default function HomePage() {
  return <AppShell />;
}
